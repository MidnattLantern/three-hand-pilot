import React from "react";

const SerialNumber = (props) => {
    const {
        id,
        owner,
        link_product_name,
        link_partnering_end,
        serial_number,
        SerialNumberList,
        SerialNumberDetail,
    } = props;

    return (
        <div>
            <h1>SerialNumber page</h1>
        </div>
    );
};

export default SerialNumber;
